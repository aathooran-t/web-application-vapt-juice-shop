# Application Enumeration and Attack-Surface Mapping

## 1. Objective

The objective of the enumeration phase was to identify the application’s accessible functions, API routes, REST endpoints, user-controlled inputs and authentication-related requests.

OWASP Juice Shop was accessed through Burp Suite in a controlled local Docker environment. This phase focused on mapping the attack surface rather than exploiting vulnerabilities.

No vulnerability was declared solely from the enumeration observations.

---

## 2. Target Information

| Item            | Details                         |
| --------------- | ------------------------------- |
| Application     | OWASP Juice Shop                |
| Target URL      | `http://127.0.0.1:3000`         |
| Environment     | Local Docker laboratory         |
| Testing system  | Kali Linux                      |
| Proxy tool      | Burp Suite Community Edition    |
| Assessment type | Authorized Web Application VAPT |

---

## 3. Burp Suite Traffic Capture

OWASP Juice Shop was accessed through Burp Suite’s built-in browser. Proxy interception was disabled during normal navigation, allowing Burp Suite to record the requests generated by the application.

The HTTP history contained application resources, API calls, REST requests and requests generated by user-facing functionality.

Evidence:

* `../screenshots/08-burp-proxy-history.png`

---

## 4. Site-Map Analysis

The target was added to Burp Suite’s scope, and the generated site map was examined.

The following application areas were identified:

```text
/api/Challenges
/api/Feedbacks
/api/Quantitys
/api/SecurityAnswers
/api/SecurityQuestions
/api/Users
/rest/admin
/rest/basket
/rest/captcha
/rest/languages
/rest/products
/rest/saveLoginIp
/rest/user
```

These endpoints were recorded as part of the application’s attack surface. Their presence alone does not establish a vulnerability.

Evidence:

* `../screenshots/burp-site-map.png`

---

## 5. Authentication Mapping

### Login Request

The login function submitted credentials using:

```http
POST /rest/user/login
```

The request used JSON parameters named:

```text
email
password
```

Invalid test credentials produced:

```http
HTTP/1.1 401 Unauthorized
```

Response message:

```text
Invalid email or password.
```

No authentication token was issued following the unsuccessful login attempt.

Evidence:

* `../screenshots/login-request.png`
* `../screenshots/login-response.png`

### Registration Request

The user-registration process generated an API request to create a new laboratory account.

The request and response were captured to identify the endpoint, HTTP method and accepted input fields. Credential values and security-answer information were redacted from the stored evidence.

Evidence:

* `../screenshots/registration-request.png`
* `../screenshots/registration-response.png`

---

## 6. Product-Search Mapping

The product-search function uses the following endpoint:

```http
GET /rest/products/search?q=
```

The `q` query parameter accepts user-controlled search input. A harmless marker was used:

```text
VAPTTEST123
```

An empty result was expected because the marker did not correspond to an existing product. This step identified an input location for later manual assessment but did not confirm a vulnerability.

Evidence, if completed:

* `../screenshots/product-search-request-response.png`

---

## 7. Endpoint Inventory

| Method | Endpoint                                | Parameters           | Authentication                  | Purpose                         |
| ------ | --------------------------------------- | -------------------- | ------------------------------- | ------------------------------- |
| POST   | `/rest/user/login`                      | `email`, `password`  | No                              | User authentication             |
| POST   | `/api/Users`                            | Registration fields  | No                              | Account registration            |
| GET    | `/rest/products/search`                 | `q`                  | No                              | Product search                  |
| GET    | `/rest/user/whoami`                     | `fields`             | Session-dependent               | Current-user information        |
| GET    | `/rest/products/{id}/reviews`           | Product ID           | No                              | Product reviews                 |
| GET    | `/rest/admin/application-version`       | None                 | Observed without authentication | Application-version information |
| GET    | `/rest/admin/application-configuration` | None                 | Further validation required     | Application configuration       |
| GET    | `/api/Quantitys`                        | Resource identifiers | Further validation required     | Product-quantity information    |

---

## Additional Function Mapping

### Product Review

The product-review function accepts user-controlled review content and associates it with a product identifier.

The server accepted the submitted review and returned:

```http
HTTP/1.1 201 Created
```

```json
{"status":"success"}
```

The review message and product identifier were recorded for later input-validation and authorization testing. No vulnerability was confirmed during enumeration.

Evidence:

* `../screenshots/product-review-request.png`
* `../screenshots/product-review-response.png`

### Add Product to Basket

Adding a product generated the following request:

```http
POST /api/BasketItems/
```

The request included:

```json
{
  "ProductId": 51,
  "BasketId": "6",
  "quantity": 1
}
```

The server returned `200 OK` and created basket-item ID `9`. These numeric identifiers were recorded for later object-level authorization testing.

Evidence:

* `../screenshots/add-basket-item-request.png`
* `../screenshots/add-basket-item-response.png`

### Basket Retrieval

Opening the basket generated:

```http
GET /rest/basket/6
```

The server returned `200 OK` with basket, user, product and basket-item identifiers. No identifiers were modified during enumeration.

Evidence:

* `../screenshots/view-basket-request.png`
* `../screenshots/view-basket-response.png`

### Complaint Submission

The complaint function generated:

```http
POST /api/Complaints/
```

The observed request contained a user identifier and a user-controlled message. The server returned:

```http
HTTP/1.1 500 Internal Server Error
```

The response referenced a database foreign-key constraint failure. This was recorded for later reproduction and investigation. It was not classified as a confirmed vulnerability during enumeration.

Evidence:

* `../screenshots/complaint-request.png`
* `../screenshots/complaint-error-response.png`


## 9. Security-Relevant Observations

The following areas were selected for later manual assessment:

* Authentication behavior
* Product-search input handling
* API authorization
* Administrative REST endpoints
* User and basket identifiers
* Product-review inputs
* Customer-feedback inputs
* Security questions and answers
* Session and token handling

These are testing hypotheses and not confirmed findings.

---

## 10. Current Status

Completed enumeration activities:

* Burp Suite traffic capture
* Target scoping
* Site-map generation
* API and REST route discovery
* Login request mapping
* Registration request mapping
* Initial product-search parameter identification
- Product-review request and response mapping
- Add-to-basket request mapping
- Basket-retrieval endpoint mapping
- Complaint endpoint and request-parameter mapping

